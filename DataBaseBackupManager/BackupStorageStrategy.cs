namespace DataBaseBackupManager
{
    /// <summary>
    /// Give options for backup storage.
    /// </summary>
    public enum BackupStorageStrategy
    {
        /// <summary>
        /// Reads and writes the backup locally.
        /// </summary>
        LocalStorage
    }
}
